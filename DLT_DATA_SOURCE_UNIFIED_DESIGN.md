# 大乐透数据源管理方案 - 集合统一优化

## 背景

在项目演进过程中，大乐透数据存储经历了多次迁移和优化，导致出现多个数据集合：
- `hit_dlts`
- `hit_dlts`
- `hit_dlts`
- 其他相关集合

## 优化方案

### 核心决策

**统一数据源：`hit_dlts`**

1. **数据源特点**：
   - 总记录数：2,792 条
   - 期号范围：7001 - 25124
   - 日期范围：2011年1月1日 - 2025年10月31日
   - 包含完整的开奖数据和统计信息

2. **迁移历史**：
   - `migrate-dlt-data.js`：重命名字段、清理数据
   - `migrate-add-dlt-statistics.js`：添加统计字段

### 实施步骤

1. **代码集合引用统一**
   - 所有代码直接使用 `hit_dlts` 集合
   - 替换所有其他集合的引用
   - 保留现有的数据结构和字段

2. **数据完整性**
   - 保留 `hit_dlts` 中的所有现有字段
   - 字段包括：
     ```javascript
     {
       ID: Number,            // 连续递增ID
       Issue: Number,         // 期号
       Red1-Red5: Number,     // 红球号码
       Blue1-Blue2: Number,   // 蓝球号码
       DrawDate: Date,        // 开奖日期
       statistics: {          // 统计字段
         frontSum: Number,
         frontSpan: Number,
         frontHotWarmColdRatio: String,
         // ... 其他统计信息
       }
     }
     ```

### 代码迁移指南

#### 查询示例

```javascript
// 推荐方式
const dltRecords = await db.collection('hit_dlts')
    .find({ Issue: { $gte: startIssue, $lte: endIssue } })
    .sort({ Issue: 1 })
    .toArray();

// 不再使用以下方式
const records = await db.collection('hit_dlts').find(...);
const records = await db.collection('hit_dlts').find(...);
```

### 性能考虑

- 已有索引：`ID`、`Issue`
- 查询性能优秀
- 数据结构稳定

### 迁移风险

1. 代码中硬编码的集合名称可能需要手动更新
2. 建议全面测试，确保所有查询正常

### 监控指标

- 迁移文件数
- 替换的集合名称
- 查询性能对比

## 文档历史

- **版本**：1.0
- **创建日期**：2025-11-16
- **最后更新**：2025-11-16

## 贡献者

- Claude Code
- 数据库优化团队

🤖 Generated by Claude Code